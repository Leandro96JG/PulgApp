---
description: Implement and verify the next unchecked Pulgapp task from plan.md.
agent: build
---

Load the `implement-pulgapp` skill. Read the configured project instructions, identify the earliest executable unchecked task in `plan.md`, and complete only that task end to end. Run focused verification, update task evidence and Current Status, and keep human hardware/game checks pending. If `$ARGUMENTS` names a task ID, execute that task only after confirming all preceding gates passed.
